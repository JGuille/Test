# Test
Helicon Test ReactJS/NodeJS/Express

## Server Setup
1. Go to the `server` folder.
2. Run `npm install`.
3. Run `node server.js` to start the API.

## Client Setup
1. In another terminal, go to the `client` folder.
2. Run `npm install`.
3. Run `npm start` to start the app.
